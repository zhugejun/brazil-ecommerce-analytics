name: 'brazil_ecommerce_analytics'
version: '1.0.0'
profile: 'brazil_ecommerce_analytics'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  brazil_ecommerce_analytics:
    +materialized: view

